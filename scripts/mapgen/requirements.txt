# Dev/build-time ONLY — used to render frontend/src/assets/bible-map.png from Natural Earth
# vectors. The committed PNG is what ships; songbird's runtime never imports these. Deliberately
# kept out of backend/requirements.txt and backend/requirements-dev.txt to keep the runtime lean
# (no heavy geo/ML stack in songbird).
pyshp>=2.3
Pillow>=10.0
